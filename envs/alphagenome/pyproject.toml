[project]
name = "hoptf-alphagenome"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "alphagenome-research @ git+https://github.com/google-deepmind/alphagenome_research.git",
    "pyfaidx",
    "numpy",
    "torch",
]
