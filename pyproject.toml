[project]
name = "hoptf"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyfaidx",
    "numpy",
    "scikit-learn",
    "matplotlib",
    "scarf",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
